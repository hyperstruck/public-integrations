# ResolvePurpose

Why a learning resolve is being performed.

Value | Meaning
--- | ---
`agent_loop` | Executing agent or agent-owned integration; a non-empty response is eligible for reporting attribution.
`explicit_recall` | Human-facing inspection; excluded from reporting attribution.
